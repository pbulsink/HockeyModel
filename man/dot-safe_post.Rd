% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/frontend-social.R
\name{.safe_post}
\alias{.safe_post}
\title{Post to social media, capturing failures instead of silently discarding them}
\usage{
.safe_post(description, ...)
}
\arguments{
\item{description}{(\code{character(1)}) Human-readable description of the post,
used in warnings and summaries if the post fails.}

\item{...}{Arguments forwarded to \code{atrrr::post()}.}
}
\value{
(\code{list}) With \code{description} (\code{character(1)}), \code{success}
(\code{logical(1)}), and \code{error} (\code{character(1)} or \code{NA_character_}).
}
\description{
Wraps a single \code{atrrr::post()} call. A bare \code{try()} (the previous
approach) suppresses any error so a failed post is indistinguishable from
a successful one; this helper instead catches the error, reports it via
\code{cli::cli_warn()} immediately, and returns a structured result so callers
can aggregate and report on failures across a batch of posts.
}
\keyword{internal}
