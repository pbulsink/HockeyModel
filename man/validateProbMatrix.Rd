% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation-helpers.R
\name{validateProbMatrix}
\alias{validateProbMatrix}
\title{Validate a Dixon-Coles probability matrix or result-probability vector}
\usage{
validateProbMatrix(x, tol = 1e-06, clamp_tol = 0.001, context = "probability")
}
\arguments{
\item{x}{(\code{numeric}) A probability matrix or vector, expected to sum to 1.}

\item{tol}{(\code{double(1)}) Tolerance for the sum-to-1 check.}

\item{clamp_tol}{(\code{double(1)}) Magnitude of out-of-range values that are
silently clamped into \verb{[0, 1]} rather than raising an error.}

\item{context}{(\code{character(1)}) Label used in error messages to identify
the caller.}
}
\value{
\code{x}, with any within-tolerance out-of-range values clamped into
\verb{[0, 1]}.
}
\description{
Checks that all entries are finite, within tolerance of
\verb{[0, 1]}, and sum to 1 (within tolerance). Small negative/over-1 values
are clamped silently -- these arise both from floating point error and
from the Dixon-Coles tau adjustment itself, which can push a boundary
cell (e.g. 0-0/1-0 goals) slightly negative for some \code{rho}/lambda/mu
combinations (see Dixon & Coles 1997). Larger violations raise an error
since they indicate a modeling bug (e.g. a mis-normalized matrix) rather
than an expected small artifact.
}
\keyword{internal}
