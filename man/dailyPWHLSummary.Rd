% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pwhl-model.R
\name{dailyPWHLSummary}
\alias{dailyPWHLSummary}
\title{Daily PWHL summary — update, predict, and post}
\usage{
dailyPWHLSummary(
  graphic_dir = file.path(getOption("HockeyModel.prediction.path",
    "./prediction_results"), "pwhl_graphics"),
  delay = stats::runif(1, min = 2, max = 6) * 60
)
}
\arguments{
\item{graphic_dir}{(\code{character(1)}) Directory to save generated PNG files.}

\item{delay}{(\code{double(1)}) Seconds to wait between social-media posts.}
}
\value{
\code{NULL} (invisibly).
}
\description{
The PWHL equivalent of \code{\link[=dailySummary]{dailySummary()}}. Fetches the latest
schedule and scores, re-fits the Dixon-Coles model, generates graphics for
today's games and season-wide predictions, and optionally posts them to
social media via \code{\link[atrrr:post]{atrrr::post()}}.

Running \code{dailyPWHLSummary()} with no arguments performs the full daily
workflow using the package's stored PWHL datasets.
}
